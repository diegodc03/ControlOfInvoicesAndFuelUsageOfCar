import { MongoClient } from "mongodb";

console.log("Iniciando el servidor...");

/*
const getConnection = async () => {
    try {
      const mongoUrl = "mongodb://localhost:27017/nodejs_test";
      const client = new MongoClient(mongoUrl, { useUnifiedTopology: true });
      await client.connect();
      return client.db(); // Devuelve la base de datos
    } catch (error) {
      console.error(error);
    }
  };
  
export {getConnection};


const getConnectionFacturas = async () => {
    try {
      const mongoUrl = "mongodb://localhost:27017/facturas";
      const client = new MongoClient(mongoUrl, { useUnifiedTopology: true });
      await client.connect();
      return client.db(); // Devuelve la base de datos
    } catch (error) {
      console.error(error);
    }
  };
  
export {getConnectionFacturas};*/